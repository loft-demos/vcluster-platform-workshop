# Private Nodes Tenancy Model

The Private Nodes tenancy model runs the virtual cluster inside its own dedicated Kubernetes host cluster—backed by physically separate nodes, a separate control plane, and separate infrastructure components like CNI and CSI drivers