# Scenario Complete

You validated a lightweight `vind` + vCluster Platform environment and ran the Shared Nodes flow.

Recommended next scenario:
- `dedicated-nodes` (separate scenario to stay within 1-hour runtime limits)
